namespace Akka.Persistence.Benchmarks;

public static class PluginTypeExtensions
{
    /*
    public static (DockerContainer, string) GetTestContainer(this PluginType pluginType)
    {
        switch (pluginType)
        {
            case PluginType.SqlServer:
            {
                return (container, container.GetConnectionString());
            }
            case PluginType.PostgreSql:
            {
                return (container, container.GetConnectionString());
            }
            case PluginType.MySql:
            {
                return (container, container.GetConnectionString());
            }
            case PluginType.MongoDb:
            {
                return (container, container.GetConnectionString());
            }
            case PluginType.Redis:
            {
                return (container, container.GetConnectionString());
            }
            case PluginType.Azure:
            {
                return (container, container.GetConnectionString());
            }
            case PluginType.Sqlite:
                throw new InvalidOperationException("Sqlite is not supported");
            default:
                throw new ArgumentOutOfRangeException(nameof(pluginType), pluginType, null);
        }
    }
    */
}